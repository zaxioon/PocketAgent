/**
 * Reserved contract boundary for a future non-vector phone-local memory backend.
 * The vector store does not instantiate or fall back to this port in the current iteration.
 */
export interface PhoneLocalMemoryBlobFallbackPort {}
